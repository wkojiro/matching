
<% unless client_logged_in? %>


<%= link_to "クライアントログイン", client_login_path %>


<% else %>
client/show.html
<%= Time.now.to_s %>
<%= datetime = DateTime.now %>
<%= Date.today %>
<h1>

  <%= @client.compname %></h1>
  <% @current_client.inspect %>
  <%= session[:id] %>


 <p>新規登録はこちら <%= link_to "原稿登録!", new_campaign_path %></p>
  <% if @client.campaigns.any? %>
   <%= render @campaigns %>
   <%# @camps = Campcat.find(campaigns.id) %>
   <%# @camps %>
   <hr>
  <% end %>
本来的にここに載せるもの
<hr>
承認待ち案件　｜　編集　｜削除

<% end %>

